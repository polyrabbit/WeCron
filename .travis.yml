language: python
python:
  - "2.7"
services:
  - postgresql
before_script:
  - psql -c 'create database wecron;' -U postgres
  - make syncdb
install:
  - pip install -r requirements.txt
script:  make test
notifications:
  email: false
